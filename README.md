
FULL Travel Website – Angular + Node Reference Implementation

Includes:
- Angular app with routing, SEO hooks, reusable UI components
- Home, Reviews (infinite scroll), Destinations, Destination Detail
- URL-synced faceted search
- Backend API with Redis caching
- Search indexing–ready endpoints

Run:
Frontend: ng serve
Backend: npm run dev
